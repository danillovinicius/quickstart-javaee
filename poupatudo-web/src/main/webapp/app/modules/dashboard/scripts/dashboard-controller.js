/* global angular */

angular.module('poupatudo.dashboard.controller', [])

    .controller('DashboardController', ['$scope', 'messageService',
        function ($scope, messageService) {
           //messageService.info({messageProperties:'MS001'});
        }
    ])

;