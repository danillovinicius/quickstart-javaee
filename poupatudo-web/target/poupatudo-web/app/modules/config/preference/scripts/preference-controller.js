/* global angular */

angular.module('poupatudo.preference.controller', [])

    .controller('PreferenceController', ['$scope', 'messageService',
        function ($scope, messageService) {
            
//           messageService.success({messageProperties:'MS001'});
        }
    ])

;